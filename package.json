{
  "name": "@targoninc/ts-logging",
  "type": "module",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "logging",
    "typescript",
    "library",
    "database"
  ],
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/targoninc/ts-logging.git"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun --format esm && tsc",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "uuidv7": "^1.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-plugin-dtsx": "^0.21.9"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
